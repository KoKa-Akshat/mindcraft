{"id":"computer-science::constant-time-o-1","name":"Constant Time O(1)","subject":"computer-science","subjectTitle":"AP Computer Science (Python Version)","level":"core","lesson":{"title":"O(1): Constant Time and the Power of Direct Access","summary":"An algorithm runs in constant time, O(1), when the number of operations it performs does not depend on the size of its input at all.","body":"Constant time, written O(1), is the fastest and simplest growth rate an algorithm can have. It means the amount of work stays the same no matter how large the input gets. Whether you hand an O(1) operation a list of 5 items or 5 billion items, it performs the same number of steps.\n\nA classic comparison is dictionary lookup versus list search in Python.\n\nstudent_grades = {\"Amy\": 92, \"Ben\": 85, \"Cal\": 78}\ngrade = student_grades[\"Ben\"]\n\nstudents = [(\"Amy\", 92), (\"Ben\", 85), (\"Cal\", 78)]\nfor name, score in students:\n    if name == \"Ben\":\n        grade = score\n        break\n\nThe dictionary lookup student_grades[\"Ben\"] uses a hash table under the hood: Python computes a hash of \"Ben\" and jumps almost directly to where its value is stored, doing roughly the same small amount of work whether the dictionary has 3 entries or 3 million. That is O(1). The list version has to walk through students one entry at a time until it finds a match, so in the worst case it does n comparisons, O(n). This comparison, dict lookup against list search, is one of the most common reasons real Python programs choose dictionaries or sets over lists when they need fast lookups by key.\n\nOther everyday O(1) operations in Python include appending to the end of a list with values.append(x), accessing a list element by index with values[3], pushing or popping from the end of a list used as a stack, and basic arithmetic. Each of these does a fixed, small amount of work regardless of how large the list is, because Python lists keep track of where their data lives and their current length, so no searching is involved.\n\nThe doubling thought experiment makes O(1) easy to spot. Take an O(1) operation that runs in, say, 0.001 milliseconds on an input of 1,000 items. Now run it on an input of 1,000,000 items, a thousand times larger. If the operation is truly O(1), it should still take roughly 0.001 milliseconds. The running time simply does not react to input size, which is the defining signature of constant time.\n\nIt is worth being precise about what O(1) does not mean: it does not mean instant or zero work. It means the work is bounded by some fixed amount that never grows, even if that fixed amount is large. A function that always loops exactly 500 times, regardless of n, is still O(1), because 500 is a constant, not a value that depends on the input. What matters for classifying time complexity is always whether the work scales with n, not the raw size of the constant.","source":"claude_direct"},"hasSim":false,"sim":null,"prereqs":[{"id":"computer-science::time-complexity","label":"Time Complexity","hasLesson":true,"hasSim":false}],"unlocks":[]}