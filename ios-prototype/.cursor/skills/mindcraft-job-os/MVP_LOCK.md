# Field Desk MVP lock (Aug 2026)

## Status: Apply today box DONE. Ready for connector testing.

### Locked in (do not reopen unless asked)
- Field Desk is post-login home
- Dock book opens **Apply today** as one **solid cream paper board** on the right
- Inner layout: neat boxes (Upload resume · Creative writing · Connect LinkedIn · + links · note) + Roles table
- Whole board is **movable + ↘ resizable**
- Starts **empty** (no personal seed). Roles unlock after resume + LinkedIn
- ACT Binder: shell + minimize + resume last question
- No em dashes in student-facing writing

### Gmail (locked direction)
- Dock **envelope** + Connect → Gmail open a **movable Gmail workflow box**
- Uses **real Gmail inbox** via Google OAuth (`gmail.readonly`)
- **Not AgentMail** (no new @agentmail.to address)

### Your next test (now)
1. Envelope icon / Connect → Gmail → Connect Gmail inbox → confirm inbox lists
2. Then Calendar · Moodle
3. Tell us what breaks / what display looks wrong

### Explicitly later (do not start yet)
1. Polish Connect feature further + data moats per option
2. Mount AI agent
3. Bottom Ask/search bar → Google-tools style search
4. Deeper Job OS / Daily Sync agent wiring

### Entry points
- Book dock → Apply today board
- Connect card on desk → Gmail / Calendar / Moodle guides
- Binder → ACT Field Book / Doc→Cook
