# Mac verify pass — paste this into a LOCAL Cursor agent on the Mac

**Why this file exists:** Field Desk Create / Projects / dash polish is on
`main` now. Pull main, Clean Build, Cmd+R on the iPad.

## Paste prompt (local Mac Cursor session)

```
cd /Users/akoirala/Developer/mindcraft && git pull origin main.

Build + install ios-prototype/MindCraftNotes to iPad
(id=00008103-0012296E01D0C01E). Product → Clean Build Folder, then Cmd+R.

Walk:
a. Jesse's top-right = Volume · lime Create · Sign out (clear, not blending).
b. Create → spatial studio (desk cream space, clear +/- zoom) → Jesse's back.
c. Projects → centered Malevolent Shrine only (no polka, no tap copy) →
   tap shrine → work desk.
d. Work desk: pinch on a card zooms that card; pinch outside zooms desk.
e. + tray Gdoc = writable whiteboard; Presentation = real slide deck.
f. Manage → hub → back.

If build fails, paste the first Xcode error. Append a note to
NATIVE_APP_BUILD_PLAN.md when done.
```

## Raw commands

```bash
cd /Users/akoirala/Developer/mindcraft && git pull origin main
open ios-prototype/MindCraftNotes/MindCraftNotes.xcodeproj
# Product → Clean Build Folder, select iPad, Cmd+R
```
