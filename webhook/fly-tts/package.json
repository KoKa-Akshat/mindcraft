{
  "name": "mindcraft-tts-flyio",
  "version": "1.0.0",
  "private": true,
  "description": "Kokoro TTS, always-on (see server.js header for why this exists as a separate service from webhook/).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "kokoro-js": "^1.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
