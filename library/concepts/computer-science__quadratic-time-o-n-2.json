{"id":"computer-science::quadratic-time-o-n-2","name":"Quadratic Time O(n^2)","subject":"computer-science","subjectTitle":"AP Computer Science (Python Version)","level":"core","lesson":{"title":"O(n^2): Quadratic Time and the Trap of Nested Loops","summary":"An algorithm runs in quadratic time, O(n^2), when it performs roughly n operations for each of n inputs, typically because a loop is nested inside another loop over the same data.","body":"Quadratic time, O(n^2), shows up whenever an algorithm compares or combines every element of an input with every other element. The most common source of quadratic time is nested loops: an outer loop that runs n times, with an inner loop that also runs about n times for each pass of the outer loop.\n\nHere is a function that checks a list of n names for any duplicates by comparing every pair.\n\ndef has_duplicate(names):\n    for i in range(len(names)):\n        for j in range(len(names)):\n            if i != j and names[i] == names[j]:\n                return True\n    return False\n\nLet's count operations directly, the same technique covered later in Counting Operations. The outer loop runs n times. For each of those n outer passes, the inner loop also runs n times. That gives n times n, or n^2, total passes through the innermost comparison, before we even count the comparison itself. Even though most individual steps, the comparison and the equality check, are cheap, there are n^2 of them, so we say this function runs in O(n^2) time.\n\nThis directly builds on the idea of nested loops: whenever you see a loop over an input sitting inside another loop over the same input, that is a strong signal of quadratic behavior. Not every nested loop is O(n^2); if the inner loop's range depended on a fixed constant instead of n, the nesting would not multiply the growth. But a loop inside a loop over the same n items, as in has_duplicate, almost always is.\n\nThe doubling thought experiment shows why O(n^2) gets dangerous fast. Suppose has_duplicate takes 1 second to check a list of 1,000 names in the worst case. Doubling the input to 2,000 names does not double the time, it roughly quadruples it, to about 4 seconds, because (2n)^2 equals 4n^2. Doubling again to 4,000 names pushes the time to roughly 16 seconds. This is very different from linear time, where doubling the input only doubles the time. Quadratic algorithms can feel perfectly fine on small test inputs and then become unusably slow the moment real world data gets large, which is exactly why recognizing O(n^2) code early matters.\n\nQuadratic time is not automatically bad. Some problems genuinely require comparing every pair of elements, and for small or moderate n, an O(n^2) algorithm can be simpler to write and perfectly fast enough. But when n can grow into the thousands or millions, the gap between O(n^2) and a faster alternative like O(n log n), covered later in this unit, becomes enormous, which is why so much of algorithm design is about finding ways to avoid unnecessary nested loops over the same data.","source":"claude_direct"},"hasSim":false,"sim":null,"prereqs":[{"id":"computer-science::time-complexity","label":"Time Complexity","hasLesson":true,"hasSim":false},{"id":"computer-science::nested-loops","label":"Nested Loops","hasLesson":true,"hasSim":false}],"unlocks":[]}